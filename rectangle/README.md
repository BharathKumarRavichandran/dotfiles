# Rectangle

## Installation
- Download and install [Rectangle](https://github.com/rxhanson/Rectangle)
- Run `./configure.sh rectangle` to import `config.json`
- To update `config.json`, use Rectangle's Export action and overwrite the file
