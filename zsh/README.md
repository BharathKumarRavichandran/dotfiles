# Zsh

Run `./configure.sh zsh` from the repo root to create symlinks. Plugins
(autosuggestions, syntax-highlighting, etc.) are managed by
[Antidote](../antidote); the prompt is [Starship](../starship). Missing optional
tools are skipped so a minimal or newly bootstrapped shell remains usable.
